services:
  - type: web
    name: osteoml
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python web/model_service.py
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: PYTHON_VERSION
        value: 3.13.1
